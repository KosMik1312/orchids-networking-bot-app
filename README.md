# Orchids Networking Bot App

Этот проект представляет собой Telegram бота для нетворкинга с встроенным miniApp Telegram приложением. Бот помогает организовывать встречи и знакомства между людьми (вдохновлено Allora).

## Структура проекта

- `src/` - Frontend приложение на Next.js (miniApp)
- `bot/` - Backend Telegram бот на Python с использованием Aiogram3
- `public/` - Статические файлы (изображения и т.д.)
- `scripts/` - Скрипты для развертывания и обслуживания

## Установка и запуск

### Требования

- Node.js (для frontend)
- Python 3.8+ (для бота)
- Bun (рекомендуется для управления зависимостями)

### Frontend (miniApp)

1. Установите зависимости:
   ```bash
   bun install
   ```

2. Запустите сервер разработки:
   ```bash
   bun dev
   ```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Backend (Telegram бот)

1. Перейдите в папку `bot/`:
   ```bash
   cd bot
   ```

2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   ```

3. Активируйте виртуальное окружение:
   - Windows: `venv\Scripts\activate`
   - Linux/Mac: `source venv/bin/activate`

4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

5. Настройте переменные окружения (создайте `.env` файл на основе примера).

6. Запустите бота:
   ```bash
   python bot.py
   ```

## Развертывание

- Frontend можно развернуть на Vercel или другом хостинге для Next.js
- Backend (бот) должен работать на сервере с постоянным интернетом

## Контрибьютинг

Пожалуйста, следуйте стандартным практикам Git и создавайте pull requests для внесения изменений.

## Лицензия

[Укажите лицензию, если применимо]
